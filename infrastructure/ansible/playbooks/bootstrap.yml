---
- name: Bootstrap lab server
  hosts: lab
  become: true
  gather_facts: true

  tasks:
    - name: Ensure basic packages are installed
      ansible.builtin.apt:
        name:
          - curl
          - whet
          - git
          - htop
          - tree
          - lsof
        state: present
        update_cache: true
