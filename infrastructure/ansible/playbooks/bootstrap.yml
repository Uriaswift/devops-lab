---
- name: Bootstrap lab server
  hosts: lab
  become: true
  gather_facts: true

  tasks:
    - name: Ensure basic packages are installed
      ansible.builtin.apt:
        name: "{{ base_packages }}"
        state: present
        update_cache: true
